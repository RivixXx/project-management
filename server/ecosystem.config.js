// Конфигурационный файл для PM2 (Process Manager 2)
// PM2 используется для управления Node.js приложениями в production среде
module.exports = {
  // Массив с описанием приложений, которыми будет управлять PM2
  apps: [
    {
      // Имя процесса, которое будет отображаться в списке процессов PM2
      name: "project-management",
      // Команда для запуска приложения (в данном случае используем npm)
      script: "npm",
      // Аргументы для команды запуска (запускаем скрипт dev из package.json)
      args: "run dev",
      // Переменные окружения для процесса
      env: {
        // Устанавливаем окружение разработки
        NODE_ENV: "development",
      },
    },
  ],
};
